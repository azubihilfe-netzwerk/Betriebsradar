#local postgres started via `docker compose up -d`, see MIGRATIONS.md
PG_URL=postgres://betriebsradar:betriebsradar@localhost:5432/betriebsradar

#mailserver credentials
SMTP_HOST=
SMTP_USER=
SMTP_PASS=
#URL needed for email verification links
FRONTEND_URL=https://betriebsradar.org