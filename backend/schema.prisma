// This file is automatically generated by Keystone, do not modify it manually.
// Modify your Keystone config when you want to change this.

datasource sqlite {
  url               = env("DATABASE_URL")
  shadowDatabaseUrl = env("SHADOW_DATABASE_URL")
  provider          = "sqlite"
}

generator client {
  provider = "prisma-client-js"
}

model User {
  id        Int       @id @default(autoincrement())
  name      String    @default("")
  email     String    @unique @default("")
  password  String
  roles     String    @default("[]")
  createdAt DateTime? @default(now())
}

model Company {
  id        Int      @id @default(autoincrement())
  name      String   @default("")
  trade     String   @default("")
  address   String   @default("")
  contact   String   @default("")
  size      String
  verified  Boolean  @default(false)
  latitude  Float?
  longitude Float?
  reviews   Review[] @relation("Review_company")
}

model Review {
  id                               Int      @id @default(autoincrement())
  name                             String   @default("")
  email                            String   @default("")
  company                          Company? @relation("Review_company", fields: [companyId], references: [id])
  companyId                        Int?     @map("company")
  collective                       Boolean  @default(false)
  hoursPerWeek                     Int?
  overtimePerMonth                 Int?
  trainingShortenable              Boolean  @default(false)
  partTime                         Boolean  @default(false)
  specialtiesOther                 String   @default("")
  ageAtEmployment                  Int?
  yearOfHiring                     String   @default("")
  yearOfLeaving                    String   @default("")
  ongoing                          Boolean  @default(false)
  genderIdentityRespected          Boolean  @default(false)
  position                         String
  listenedTo                       String?
  canAskColleagues                 String?
  canAskBoss                       String?
  tone                             String?
  explained                        String?
  proximity                        String?
  boundariesRespected              String   @default("[]")
  appreciated                      String?
  experienceText                   String   @default("")
  languages                        String   @default("")
  gender                           String?
  sharedWithCompany                String?
  feltComfortableSharing           String?
  disabilityTypes                  String   @default("[]")
  disabilitySharedWithCompany      String?
  disabilityFeltComfortableSharing String?
  ethnicityTypes                   String   @default("[]")
  ethnicitySharedWithCompany       String?
  ethnicityFeltComfortableSharing  String?
  needsRespected                   String?
  feedback                         String   @default("")
  moreWishes                       String   @default("")
  status                           String?  @default("awaitingReview")
  emailVerified                    Boolean  @default(false)
  accessKey                        String   @unique @default("")

  @@index([companyId])
}
